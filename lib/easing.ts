// Shared cubic-bezier easing, typed as a 4-tuple so it satisfies
// framer-motion's `Easing` type (a bare number[] literal does not).
export const EASE_OUT: [number, number, number, number] = [0.22, 1, 0.36, 1];
